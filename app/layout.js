export const metadata = {
  title: 'FX Date Calculator | KustodyFi',
  description: '외환 파생상품 Date Rule 계산기',
}

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  )
}
